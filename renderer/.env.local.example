# Express API the renderer reads blueprints from (server-side only)
CAPABLE_API_URL=http://localhost:5000

# Public apex domain — used for subdomain routing and OG URLs
NEXT_PUBLIC_APP_DOMAIN=capable.app
